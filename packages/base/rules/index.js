const allRules = require('./all');

const markdownCodeRules = require('./md-code');

const prettierRules = require('./prettier');

module.exports = {
  allRules,
  markdownCodeRules,
  prettierRules
};
